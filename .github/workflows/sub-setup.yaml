# ⚠️ DEPRECATED: This workflow is deprecated and will be removed in a future version.
# Please use the .github/actions/setup action instead in your workflows.
#
# For same repo: uses: ./.github/actions/setup
# For cross-repo: uses: navigaite/github-organization/.github/actions/setup@main
---
on:
  workflow_call: {}

permissions: {}

jobs:
  setup:
    name: 🗂️ Setup Environment
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: 🗂️ Next.js Cache
        uses: jongwooo/next-cache@v1

      - name: 📦 Install dependencies
        run: npm ci
